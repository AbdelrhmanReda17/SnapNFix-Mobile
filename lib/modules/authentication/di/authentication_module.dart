import 'package:injectable/injectable.dart';

@module
abstract class AuthenticationModule {
  // This is just a marker class to include all auth submodules
  // Actual registration happens in the submodules
}
