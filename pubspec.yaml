name: snapnfix
description: "SNAPNFIX"

publish_to: 'none'


version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  flutter_native_splash: ^2.0.9
  flutter_localizations:
    sdk: flutter
  intl: ^0.19.0
  flutter_screenutil:




dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter_gen:
  output: lib/l10n
  line_length: 80

flutter:
  generate: true
  uses-material-design: true

assets:
  - assets/images/splashFrame.png